import React from 'react';

import { ProcessModel } from 'models/ProcessModel';

export const usePlanificationSolver = (incomingProcess:ProcessModel[]):void => {

    incomingProcess.forEach((process:ProcessModel) => {
        

    });


}